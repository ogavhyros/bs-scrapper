# ── Business Scout Environment Variables ──────────────────────────────────────

# Backend server port (default: 3001)
PORT=3001

# Optional: pre-fill your API key server-side (not required — you can enter it in the UI)
# GOOGLE_PLACES_API_KEY=AIzaSy...
